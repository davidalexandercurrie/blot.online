import React from 'react';
import '@fontsource/open-sans';
import { footerContainer } from '../styles/Layout.module.css';

const Footer = () => {
  return <footer className={footerContainer}></footer>;
};

export default Footer;
