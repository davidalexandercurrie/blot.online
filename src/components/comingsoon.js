import React from 'react';

const styles = {
  fontSize: '2rem',
  top: '45%',
  left: 'calc(50% - 10rem)',
  position: 'fixed',
  width: '20rem',
};

const ComingSoon = () => {
  return <div style={styles}>Issue 1 Coming Soon</div>;
};

export default ComingSoon;
